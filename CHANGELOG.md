# 변경 내역

## 2026-09-02 — P7 · 안정화와 납품 준비

`npm run verify` 로 한 번에 도는 검증 스위트를 만들고, 그것으로 찾아낸 것을
고쳤습니다. 아래 수치는 전부 실제로 재거나 확인한 값입니다.

### 권한 재정비 — 실제로 새고 있었습니다

**일반 직원 계정으로 근태대장을 열었더니 115명 전원의 근태가 그대로 나왔습니다.**
메뉴는 감춰져 있었지만 자료는 막혀 있지 않았습니다. 화면에서 메뉴를 감추는 것은
안내이지 통제가 아닙니다 — 서버 액션은 주소만 알면 직접 부를 수 있습니다.

`src/lib/actions/read-scope.ts` 를 두고 규칙을 한 곳에 모았습니다. **인사
담당이 아니면 자기 것만 봅니다.**

- 근태·휴가·연차 조회를 본인 범위로 좁혔습니다.
- 인사정보는 이름·부서·직급까지만 나갑니다. 급여·계좌·주민번호·집주소·개인
  연락처는 인사 담당과 본인에게만 나갑니다.
- 대장(근태대장·연차대장·인력대장·급여 화면)은 관리 도구라 화면 자체를
  인사 담당 전용으로 두었습니다.

가드가 아예 없던 서버 액션도 채웠습니다. 그중 `previewBulkResign` 은 사람마다
퇴직금과 연차 정산액을 계산해 돌려주는데 아무나 부를 수 있었습니다. 그 밖에
`startEmployeeProcess`, 발령 이력 조회 3종, 급여 기준값 조회 2종, 화면 배치
저장, 로고 목록, `syncCurrentSalaries`.

### 주민등록번호 — 암호화·마스킹·열람 기록

개인정보보호법 제24조의2(시행령 제21조의2)는 암호화 저장을 정하고 있는데,
`employees.resident_number` 는 평문 컬럼이었습니다. 백업 파일 하나가 새면 전
직원의 주민번호가 그대로 나갑니다.

- **저장은 AES-256-GCM.** 열마다 난수 IV 를 써 같은 번호도 매번 다른 암호문이
  됩니다.
- **화면은 마스킹이 기본** — `900101-1******`. 목록·상세 어디에도 전체가
  나가지 않습니다.
- **전체 열람은 별도 요청 + 사유 + 기록.** 30초 뒤 자동으로 다시 가려집니다.
  한 번에 한 사람만 열립니다 — 여러 명을 한 번에 열면 목록 내려받기와 다를 바가
  없고 감사로그도 한 줄로 뭉개집니다.
- **마스킹 값이 되돌아와 진짜 번호를 덮는 사고를 막았습니다.** 화면은 별표가
  섞인 값을 들고 있는데, 그 칸을 건드리지 않고 저장하면 별표가 그대로 올라옵니다.
  되돌릴 수 없는 손실이라 별표가 있으면 저장에서 뺍니다.
- 키는 `RESIDENT_NUMBER_KEY`. 없으면 `SESSION_SECRET` 에서 파생합니다.
  **잃어버리면 복구할 수 없으니 백업 절차에 포함하세요.**

### 감사로그 — 기록하는 곳이 하나도 없었습니다

표는 전부터 있었고 "거의 모든 상호작용에 기록된다"는 주석까지 붙어 있었지만,
부르는 데가 없어 로그 화면은 언제나 비어 있었습니다. 게다가 화면 쪽에 있던
쓰기 함수는 위치가 잘못됐습니다 — 화면을 거치지 않은 호출은 흔적이 남지 않습니다.

기록을 **서버 액션 안**으로 옮기고, 직원 생성·수정, 급여 저장·확정, 급여 기준액
변경, 근태 일괄 등록, 주민번호 열람·변경에 붙였습니다. 값 자체는 남기지
않습니다 — 감사로그가 개인정보 사본이 되면 안 됩니다.

### 시드가 소속 이력을 지우고 있었습니다

`employee_assignments` 가 **0행**이었습니다. 시드가 이 표를 지우기만 하고 다시
만들지 않아, 로스터를 재적재할 때마다 115명의 최초 배치 구간이 통째로
사라졌습니다. 화면에서는 부서가 `employees` 사본에서 나와 멀쩡해 보여
발견이 늦었습니다. 발령 이력과 과거 시점 조직도 조회가 죽어 있던 상태입니다.

### 측정값

| 항목 | 실측 | 기준 |
|---|---:|---:|
| 급여 계산 115명 | 16ms | — |
| 급여 계산 300명 | 5ms | 30초 |
| 급여 계산 1,000명 | 15ms | — |
| 화면에서 115명 일괄 계산 | 1.1초 | 30초 |
| 직원 전체 질의 (115행) | 298ms | 500ms |
| 근태 전체 질의 (2,508행) | 39ms | 1,500ms |
| 엑셀 1,000행 × 25열 | 195ms | 3초 |
| 엑셀 10,000행 × 25열 (1.2MB) | 1.5초 | 15초 |
| 근태 일괄 등록 2,508건 | 1초 | — |

**그리드 1만 행은 검증하지 못했습니다.** 실데이터가 115행뿐이라 신뢰할 만한
수치를 낼 수 없었습니다. 합성 데이터로 재는 것은 의미가 있지만 이번에는 하지
않았고, 미검증으로 남깁니다.

### 검증 스위트

```bash
npm run verify
```

세 갈래로 돕니다 — 보안(24건), 자료 무결성(15건), 성능(9건). 서버를 먼저
내려야 합니다.

현재 유일한 실패는 **림부** 한 건입니다. 여치호와 명부에 같은 휴대폰 번호로
적혀 있어 번호를 비워 두었고, 근태는 번호로 붙기 때문에 그 사람의 근태가
들어오지 않습니다. 실제 번호를 넣으면 사라집니다.

### 사고 기록 — 데이터베이스를 한 번 깨뜨렸습니다

검증 도중 **서버가 뜬 상태로 쓰기 스크립트를 돌려 PGlite 데이터 파일을
손상**시켰습니다. PGlite 는 한 프로세스만 데이터베이스를 열 수 있는데 그것을
어겼습니다. 앱에서 재직 인원이 0명으로 나왔습니다.

복구했습니다 — 넣은 자료가 전부 스크립트와 기능으로 재현 가능했기 때문입니다.
마이그레이션 → 시드 → 급여 기준액 기준표 → 근태 일괄 등록(2,508건, 1초) 순으로
되살렸고, **9월 급여 총 지급액이 407,100,000원으로 손상 전과 정확히 일치**하는
것을 금액 대사로 확인했습니다.

`npm run verify` 는 시작할 때 남아 있는 `.pglite/postmaster.pid` 를 지우고
서버를 끄라고 알립니다. 강제 종료 뒤 남은 이 잠금 파일 때문에 열리지 않는 것을
손상으로 착각하기 쉽습니다.

### 납품 패키지

`RESIDENT_NUMBER_KEY` 를 `.env.example`·`docker-compose.yml`·`npm run setup`
에 넣었습니다. `npm run build` 와 `npm run build:scripts` 는 확인했지만
**Docker 이미지 빌드 자체는 이 컴퓨터에 Docker 가 없어 검증하지 못했습니다.**

## 2026-09-02 — P6 · 메뉴 재편, 홈을 업무 큐로, 설정 6그룹

### 메뉴 5개 + 설정

7그룹 15메뉴를 **5개 업무 메뉴와 설정 하나**로 접었습니다. 인사 업무를 모르는
담당자에게 열다섯 개 메뉴는 어디서 무엇을 하는지 찾는 일 자체가 일이 됩니다.

| 메뉴 | 하위 화면 |
|------|-----------|
| 홈 | 마이페이지 |
| 인력관리 | 인력대장 · 입사·퇴사 · 인사발령 · 퇴직정산 |
| 조직관리 | 사업장 배치 |
| 근태·휴가 | 근태대장 · 근태 일괄 등록 · 연차대장 · 휴가 신청·승인 · 근태 마감 |
| 급여관리 | 급여 기준액 · 급여 계산 · 급여대장 · 퇴직금 · 연말정산 |
| 설정 | 감사로그 |

하위 화면은 **그 메뉴 안에 있을 때만** 펼칩니다. 전부 펼치면 스무 줄이 넘어
원래 문제로 돌아가고, 접어 두기만 하면 "급여대장이 어디 있는지" 찾지 못합니다.
그룹 라벨은 없앴습니다 — 메뉴가 여섯이면 라벨이 항목보다 많아집니다.

전자결재와 감사로그는 독립 메뉴에서 뺐습니다. 결재는 헤더 알림과 홈 카드에서
들어가고, 감사로그는 매일 보는 화면이 아니라 설정 안에 둡니다.

글로벌 검색(⌘K)에 하위 화면을 넣었습니다. 담당자는 "급여대장"으로 찾지
"급여관리"로 찾지 않습니다.

**권한은 메뉴 단위로 주되 하위 화면 경로까지 덮습니다.** 연차대장은 `/leave`
인데 메뉴는 근태·휴가(`/attendance`) 아래라, 메뉴 목록만 대조하면 사이드바에는
보이는데 누르면 홈으로 튕깁니다. 실제로 그렇게 되어서 고쳤습니다.

### 홈 — 업무 큐

예전 홈은 인원 통계와 부서별 차트였습니다. 보기에는 좋지만 담당자가 아침에
열어서 얻는 것이 없습니다. 카드마다 **할 일과 건수, 그 일을 하는 화면으로 가는
링크**만 둡니다.

입사 처리 · 퇴사 처리 · 급여 마감(D-n) · 근태 마감 · 계약·수습 · 휴가·연차 촉진 ·
결재. 건수가 0이면 회색으로 가라앉히고, 밀린 것이 있으면 카드째 붉게 올립니다.

숫자는 그대로 쓰지 않고 뜻이 읽히게 씁니다 — 계산 완료는 `0/115`로,
마감 여부는 0과 1이 아니라 **열려 있음 / 마감됨**으로 적습니다.

### 설정 6그룹

19개 탭이 두세 줄로 감겨 있었고, 이름만 보고는 "근무설정"과 "근태유형설정"이
어떻게 다른지 알 수 없었습니다. **회사 · 조직 · 근무 · 급여 · 권한 · 시스템**
여섯 그룹으로 접어 왼쪽에 세웠습니다. 한 그룹에 많아야 넷이라 한 번에 다섯 개
이하만 읽으면 됩니다. 항목마다 한 줄 설명을 붙였습니다.

`/settings?tab=payroll-rates` 처럼 **링크로 바로 열립니다.** 매뉴얼과 다른
화면에서 특정 설정을 가리키는데, 매번 "설정에 들어가 급여 탭을 누르고"라고 쓸
수는 없습니다.

## 2026-09-02 — 근태 일괄 등록, 휴대폰 번호 필수

### 왜 휴대폰 번호인가

**근태기록에는 사원번호가 없고 휴대폰 번호가 필수 항목입니다.** 그래서 외부 근태
자료를 직원에게 붙이는 열쇠는 휴대폰 번호 하나뿐입니다. 이메일은 대신하지 못합니다 —
현장 시급직에게는 회사 이메일이 없어 명부의 이메일 칸이 비어 있는 사람이 많습니다.

- 사원 등록·수정 화면에서 **휴대폰 번호를 필수**로 받습니다. 형식(010-0000-0000)도
  검사하고, 왜 필요한지 입력칸 아래에 적어 두었습니다.
- 같은 번호를 두 사람이 쓰면 저장을 거부합니다. 번호가 겹치면 그 근태를 누구
  것으로 붙일지 판정할 수 없습니다. 서버에서도 막습니다 — 화면만 막으면
  가져오기나 스크립트로 들어옵니다.
- `drizzle/0014_phone_unique.sql` — 숫자만 남긴 값에 유일 인덱스. 표기가
  010-1234-5678 / 01012345678 / +82-10-1234-5678 로 제각각이라 컬럼 값이 아니라
  정규화한 값에 겁니다.
- 인력대장에서 휴대폰 열을 기본으로 보이게 했습니다. 비면 **미등록**으로 뜹니다.
- 이메일 라벨의 별표(`*`)를 뗐습니다. 실제로는 필수가 아닌데 필수처럼 보였습니다.

**NOT NULL 은 걸지 않았습니다.** 명부에 번호가 중복 기재된 사람이 실제로 있어
(여치호·림부가 010-6202-0593 을 함께 사용), 데이터베이스에서 막으면 그 사람을
등록하지 못하거나 가짜 번호를 넣게 됩니다. 비어 있는 편이 낫습니다 — 비면 근태가
안 붙고, 안 붙으면 눈에 띄어 고치게 됩니다.

### 근태 일괄 등록 (신규)

`/attendance/import` — 근태기기나 엑셀에서 뽑은 표를 통째로 붙여넣습니다.
CSV·TSV 파일도 받습니다.

- **머리글을 보고 열을 찾습니다.** 열 순서는 기계마다 다릅니다. 순서를 정해 놓고
  그대로 넣으라고 하면 담당자가 엑셀에서 열을 옮기다 틀립니다. 못 찾으면 화면에서
  직접 지정합니다.
- 날짜는 `2026-09-01` `2026.9.1` `20260901` `9/1` 을 모두 받습니다.
- 실근로시간 열이 없으면 **출퇴근 시각에서 계산**합니다. 근로기준법 제54조에 따라
  8시간 넘으면 1시간, 4시간 넘으면 30분을 휴게로 뺍니다. 근태기기가 이미 휴게를
  뺀 값을 준다면 끌 수 있습니다. 자정을 넘긴 야간근무는 다음 날로 봅니다.
- **저장 전에 반드시 미리보기**를 거칩니다. 줄마다 신규·수정·변화없음·오류를
  판정해 보여 줍니다. 미리보기와 저장이 같은 함수를 씁니다 — 화면에서 확인한
  결과와 실제 저장되는 내용이 갈라지면 확인의 의미가 없습니다.
- 틀린 줄은 건너뛰고 멀쩡한 줄만 넣습니다. 하나 틀렸다고 전부 되돌리면 담당자가
  2,500줄에서 오타 한 곳을 찾아 다시 붙여넣어야 합니다.
- 걸러내는 것: 번호를 쓰는 직원 없음 · 번호 겹침 · 자료 안 같은 사람 같은 날짜
  중복 · **마감된 달** · 날짜 해석 실패. 자료의 이름이 시스템과 다르면 저장은
  하되 표시합니다.
- 화면 맨 위에 **휴대폰 번호가 없거나 겹치는 사람**을 먼저 띄웁니다. 조용히 빠지면
  급여까지 가서야 드러납니다.
- 쓰기는 묶어서 보냅니다. 한 줄에 질의 하나씩 보내면 2,500줄에 40초가 걸립니다.
  묶으니 1초입니다.

### 명부 휴대폰 번호 적재

비상연락망의 번호를 114명에게 넣었습니다. 형식 오류 0건, 중복 0건.
림부는 여치호와 번호가 겹쳐 비워 두었고 화면에 미등록으로 뜹니다.

### 2026년 9월 근태 적재

> **이 근태는 실제 기록이 아닙니다.** 평일 8시간 정상근무로 채운 표준값입니다.
> 실제 근태기록으로 바꿔야 합니다. 근태 일괄 등록에서 덮어쓰면 됩니다.

114명 × (평일 20일 + 추석 평일 2일) = 2,508줄을 위 기능으로 넣었습니다.
08:00~17:00 에서 휴게 1시간을 빼 8시간, 추석 9/24·25 는 휴일 0시간입니다.
저장 2,488건, 오류 0건.

9월 급여 계산 115명 — 총 지급 **407,100,000원** (근태 반영 전 275,640,000원).
박성진 검산: 12,500 × 160시간 + 주휴 440,000 + 식대·교통비 400,000 = 2,840,000원.

## 2026-09-02 — 급여 기준액 115명 일괄 적용

> **이 금액은 실제 급여가 아닙니다.** 직군·직급별 기준표로 채운 값이고, 실제
> 계약 금액으로 바꿔야 합니다. 급여 기준액 대장에서 개인별로 고치면 됩니다.

기준액이 전부 0원이면 급여 계산이 돌아가도 결과가 의미가 없어 시스템을 검증할 수
없었습니다. 실제 금액을 받기 전까지 쓸 수 있는 값을 넣었습니다.

2026년 최저임금 10,320원(월 209시간 환산 2,156,880원)을 하한으로 잡았고,
식대·교통비 40만원은 별도입니다.

| 직급 | 사무직 | 현장관리직 |
|------|-------:|----------:|
| 사원 | 2,400,000 | 2,500,000 |
| 주임 | 2,700,000 | 2,800,000 |
| 대리 | 3,100,000 | 3,200,000 |
| 과장 | 3,700,000 | 3,800,000 |
| 차장 | 4,300,000 | 4,400,000 |
| 부장 | 5,000,000 | 5,100,000 |
| 수석부장 | 5,600,000 | 5,700,000 |
| 상무 | 7,000,000 | 7,100,000 |
| 전무 | 8,500,000 | 8,600,000 |
| 대표이사 | 12,000,000 | — |

현장 시급직 56명은 **12,500원**(최저임금 대비 +21%). 현장직을 사무직보다 10만원
높게 둔 것은 교대·현장 근무를 감안한 것입니다. 근속 가산은 넣지 않았습니다 —
개인차라서 표로 만들면 오히려 틀립니다.

적용일은 2026-09-01입니다. 그 이후 입사자는 입사일부터 적용했습니다 — 재직 전
구간을 만들면 이력이 사실과 달라집니다. 기존 0원 구간은 2026-08-31로 닫혔습니다.

확인: 구간 겹침 0건, 열린 구간 115건(직원 수와 일치), 기준액 0원 재직자 0명,
최저임금 미달 0명. 9월 급여 계산 115명 완료.

## 2026-09-02 — 주휴수당 (관리자 선택 방식)

주휴수당은 회사마다 처리가 갈립니다. 월급에 이미 포함했다고 보는 곳, 근태를 보고 주마다
산정하는 곳, 사규로 정액을 정해 둔 곳이 모두 있고 어느 쪽도 틀리지 않습니다. 코드에
하나를 박아 두면 나머지 회사에서는 이중지급 아니면 미지급이 됩니다. 그래서 **방식 자체를
관리자가 고르게** 했습니다.

### 설정 > 급여 기준값 > 주휴수당

연도별 기준값 안에 들어갑니다. 요율처럼 해가 바뀌면 정책도 바뀔 수 있고, 지난달 급여를
다시 돌릴 때는 그때의 방식으로 계산되어야 합니다.

| 방식 | 계산 |
|---|---|
| 월급에 포함 | 0원. 209시간에 주휴시간이 이미 들어 있다고 봅니다. |
| 자동 산정 | 소정근로일을 개근한 주마다 통상시급 × 1일 소정근로시간. |
| 월 정액 | 근태와 무관하게 매월 같은 금액. |

곁들인 옵션 — **적용 대상 급여방식**(기본값 시급제·일급제), **최소 주 소정근로시간**
(근로기준법 제18조 제3항의 15시간), **개근한 주에만 지급**(시행령 제30조),
**단시간 근로자 비례 계산**.

기본값은 자동 산정 + 시급제·일급제입니다. 현장 시급직이 있는 회사라서 그렇습니다.
월급제를 적용 대상에 켜면 이중지급이 될 수 있다는 경고를 화면에 붙여 두었습니다.

### 계산 근거를 명세서에 씁니다

금액만 찍히면 담당자가 직원에게 설명하지 못합니다. 급여 상세 내역에 산식을 같이 씁니다.

    + 주휴수당: 475,200원
      (13,500(통상시급) × (09-01 6.4h + 09-07 8h + 09-14 8h + 09-21 8h + 09-28 4.8h
       = 35.2시간) = 475,200원)

주마다 시간이 같으면 `× 8시간 × 4주`로 줄여 쓰고, 다르면 주별로 풀어 씁니다. 달 경계에
걸린 주와 단시간 근로자는 주별 시간이 달라지는데, 한 주의 시간을 전체에 곱한 것처럼 적으면
금액과 설명이 어긋납니다.

### 같이 고친 것

- **공휴일이 주휴수당을 깎던 문제.** 추석이 낀 주가 소정근로 3일로 잡혀 비례계산에
  걸렸습니다. 공휴일은 유급휴일이라 소정근로일에서 빠질 뿐 그 주의 계약 근로일을 줄이지
  않습니다. 비례계산은 단시간 근로자용인데 통상근로자에게 적용되고 있었습니다.
  쉬라고 준 날 때문에 임금이 줄어드는 상태였습니다.
- **공휴일 근무가 기본급에 1.0배로 들어가던 문제.** 상태가 `holiday`인 기록만 갈라내서,
  추석에 나와 일한 날이 평일 근무처럼 기본급에 섞였습니다. 근태대장은 그 날을 근무일에서
  빼는데 급여는 더해, 두 화면의 근무일수도 어긋났습니다. 이제 휴일근로(1.5배)로 갑니다.
- **가산수당 줄의 시급 표시.** 화면에서 기본급÷209로 되짚고 있었습니다. 시급직의 기본급은
  실근로시간의 곱이라 209로 나눠도 원래 시급이 나오지 않습니다(13,500원이 10,335원으로
  보였습니다). 계산에 실제로 쓰인 통상시급을 그대로 씁니다. 가산율도 하드코딩 1.5/0.5가
  아니라 기준값을 읽습니다.
- **설정 화면의 부동소수점 찌꺼기.** `3.5450000000000004`, `0.8999999999999999`가
  그대로 보였습니다.

### 확인한 값 (2026년 9월, 박성진 · 시급 13,500원)

    기본급    13,500 × 160h (평일 20일)              = 2,160,000
    휴일수당  13,500 × 1.5 × 16h (추석 9/24·25)      =   324,000
    주휴수당  13,500 × 35.2h (5주, 추석 주 8h 온전)   =   475,200
    식대·교통비 (비과세)                              =   400,000
    총 지급액                                        = 3,359,200

방식을 "월급에 포함"으로 바꾸면 주휴수당 줄이 사라지고 2,884,000원이 됩니다 —
차이 475,200원. 옵션이 표시가 아니라 계산을 바꾼다는 확인입니다.

## 2026-09-02 — 급여 기준액 입력과 급여 이력

급여액이 전부 0이라 계산이 실제로 돌지 않는 상태였습니다. 입력 수단과 **이력**을 함께
넣었습니다 — 급여는 "지금 얼마"가 아니라 "언제부터 얼마"여야 소급 인상과 과거 급여
재산출이 됩니다.

### 급여 이력 (신규)

`drizzle/0013_add_employee_salaries.sql` — 소속 이력과 같은 구간 구조입니다. 구간은
겹치지 않고, 새 구간을 열면 직전 구간이 그 전날로 닫힙니다. `employees`의 금액 컬럼은
오늘 유효한 구간의 사본입니다. 기존 115명은 입사일부터 시작하는 첫 구간으로 소급 생성했습니다.

- `fetchSalariesAsOf(date)` — 그날 유효했던 금액. **급여 계산이 이 함수를 씁니다.**
  4월에 3월분을 다시 돌려도 3월 단가가 나옵니다.
- 미래 발효 인상을 미리 등록해도 오늘 급여는 바뀌지 않습니다.
- 같은 날짜로 두 번 저장하면 구간을 갈라지 않고 고칩니다 — 오타 수정이 이력을 더럽히지
  않게 하기 위해서입니다.
- 급여는 조회 자체가 민감해서 읽기도 HR 역할로 제한합니다.

### 급여 기준액 화면 (신규)

`/payroll/salaries` — 115명을 한 명씩 사원카드에서 넣게 하면 아무도 끝까지 하지 않습니다.
대장 하나에 모아 놓고 엑셀에서 금액 열만 복사해 붙여넣습니다.

- 월급제는 월 기본급을, 시급제는 시급을 같은 열에서 받고 **단위를 옆에 표시**합니다.
- **적용일**을 위에서 정하고, 입력한 금액은 그 날짜부터 적용됩니다.
- **최저임금 미달**을 표시합니다. 막지는 않습니다 — 수습기간 감액 같은 예외가 있습니다.
  기준액은 연도별 기준값에 넣어 두었고 **고시값 대조 전**입니다.
- 미입력 인원을 세어 경고하고, "미입력만" 필터로 남은 사람만 볼 수 있습니다.

### 확인한 것

5명분을 붙여넣어 저장한 뒤 9월 급여를 계산했습니다.

| 대상 | 기준액 | 지급 | 공제 | 실수령 |
|------|--------|------|------|--------|
| 김동현 (사무직) | 월 4,500,000 | 4,900,000 | 858,371 | 4,041,629 |
| 장현표 (사무직) | 월 3,800,000 | 4,200,000 | 682,818 | 3,517,182 |
| 박성진 (현장 시급직) | 시급 13,500 | 528,250 | 22,939 | 505,311 |

박성진은 근태대장에 하루치(9.5시간)만 입력된 상태라 `13,500 × 9.5 = 128,250`에 식대·교통비
40만 원이 더해진 금액입니다 — 근태 미입력이 그대로 급여에 드러납니다.

2월분을 계산하면 같은 사람들이 40만 원으로 나옵니다. 9월 적용 인상이 2월에는 유효하지
않기 때문이며, 시점 조회가 의도대로 동작한다는 뜻입니다.

## 2026-09-02 — 중소기업용 개편 5단계 (근태대장 · 연차대장)

현장 시급직 56명이 들어오면서 **근태 실근로시간이 곧 급여**가 되었습니다. 이 단계의 축은
근태 입력의 정확도와, 그 값이 급여로 이어지는 경로입니다.

### 근태대장 (신규)

`/attendance/register` — 월 × 일 매트릭스. 종이 근태대장과 같은 배치라 담당자가 새로
배울 것이 없습니다.

- 한 칸에 **근태코드 한 글자**(○ 정상 · 연 연차 · 반 반차 · 결 결근 · 출 출장 · 교 교육 …)
  또는 **실근로시간을 숫자로** 적습니다. 코드 체계는 구 ERP `HU033 근태코드`에서 자주
  쓰이는 것만 추렸습니다.
- 주말·공휴일은 자동으로 흐리게 표시하고 소정근로일에서 뺍니다.
- 오른쪽에 근무일·실근로·연차·결근·**미입력** 집계가 붙습니다.

**빈 칸은 0시간이 아니라 "미입력"입니다.** 둘을 같게 보면 입력이 덜 된 달이 조용히 마감되어
시급직 급여가 0원으로 나갑니다. 그래서 미입력을 따로 세고, 시급직의 미입력 칸은 배경색으로
드러냅니다.

### 급여가 실제 근태를 읽습니다

`buildAttendanceData()`가 기록이 없으면 **가짜 근태를 만들어 채우고 있었습니다**
(`generateMockAttendance` — 사번 문자로 유사난수를 만들어 근무일 20~22일, 연장 0~15시간을
지어냄). 월급제에서는 눈에 띄지 않았지만 시급제에서는 지어낸 숫자가 그대로 기본급이 됩니다.
제거하고 실제 기록만 쓰도록 했고, 기록이 없으면 0으로 둡니다.

급여계산 2단계에 **근태 기록이 하나도 없는 사람**을 경고로 띄우고, 시급직이 섞여 있으면
기본급이 0원이 된다는 점을 명시합니다. 근태대장으로 바로 가는 버튼도 붙였습니다.

### 연차대장 (신규)

`/leave/register` — 개인별 부여·사용·잔여·소진율을 한 화면에.

- **법정 일수와 실제 부여 일수를 나란히** 놓고 차이를 표시합니다. 근로기준법 제60조로
  입사일에서 계산한 값과 어긋나면 부여 누락이거나 별도 부여인데, 어느 쪽이든 담당자가
  알아야 하는 차이입니다.
- 소진 50% 미만인 사람을 추려 **연차 사용 촉진**(제61조) 대상을 뽑습니다.

실제 입사일이 들어오면서 법정 일수가 사람마다 달라졌습니다 — 2012년 입사자 21일부터
올해 입사자 3일까지.

### 버그 수정

- 그리드의 인라인 편집 필드 타입이 `keyof T`로 묶여 있어, 근태대장처럼 **열이 데이터에
  따라 만들어지는 화면**(d1…d31)은 편집을 붙일 수 없었습니다. 그리드는 이 값을 해석하지
  않고 `onEdit`으로 넘기기만 하므로 문자열로 풀었습니다.

## 2026-09-01 — 실제 인력 명부 115명 적재

출처: 사내 `비상연락망 2026.09.01` 워크북의 `비상연락망 상세` 시트. 같은 서식의 블록 5개가
가로로 이어 붙은 형태라 한 줄씩 펼쳐 정규화했습니다. 입사일 파싱 실패 0건.

이전 55명(메일 명부 기준, 입사일·부서 전부 자리표시)을 대체합니다.

### 사번 — `DA` + 입사일 + 순번

`DA260818002` = 대한오토텍 + 2026-08-18 입사 + 그날의 두 번째. 순번은 명부에 적힌 순서를
따릅니다.

### 직군 3분류

사용자 지시에 따라 현장을 둘로 나눴습니다 — `office`(사무직 41) · `field_manager`(현장관리직 18) ·
`field`(현장 시급직 56). 반장·직장·과장급 현장관리직은 월급제, 현장 시급직만 시급제입니다.
둘을 한 값으로 묶으면 급여방식이 섞여 대장에서 구분할 수 없습니다.

### 이메일을 선택 항목으로

`employees.email`이 NOT NULL이라 회사 계정이 없는 현장 근로자를 넣을 수 없었습니다. 가짜
주소를 만들어 채우면 언젠가 실제로 메일이 발송되므로, 컬럼을 nullable로 바꿨습니다
(`drizzle/0012_email_optional.sql`). unique는 유지합니다 — PostgreSQL은 NULL 중복을 허용합니다.
명부에서 확인된 52명만 채우고 나머지는 비웠습니다.

### 버그 수정

- **그리드의 드롭다운 필터가 전혀 걸리지 않았습니다.** 셀은 라벨('사무직')을 보여 주는데
  필터는 옵션 값('office')과 비교하고 있어, 부서·직급·직책·고용형태·상태·직군 필터가
  모두 0건을 냈습니다. 라벨과도 대조하도록 고쳤습니다.
- **`--force` 재적재가 외래키 제약에 막혔습니다.** 삭제 순서에 연차 잔액·급여·소속 이력
  등이 빠져 있어, 한 번이라도 급여를 돌린 뒤에는 명부를 다시 넣을 수 없었습니다.
- **데모 시드가 새 컬럼을 넣지 않았습니다.** 직군·급여방식·시급이 insert 목록에 없어
  115명 전원이 사무직·월급제로 들어갔습니다.

### 확인이 필요한 항목

| 항목 | 상태 |
|------|------|
| 부서 | 명부 비고에서 확인된 39명만 배정. 생산 블록 비고는 체류자격이라 부서로 쓰지 않음 |
| 직급·직책 | 명부 '직위'에서 직급으로 읽히는 값만. '생'·'검'·'청소'는 사원/팀원 + 비고 보존 |
| 급여액 | 명부에 없어 전부 0. 시급·월급을 입력해야 급여 계산이 됩니다 |
| 김대형(대표이사) | 입사일이 비어 있어 사번 미채번 |
| 직군 미지정 10명 | 검사·품질 9명(블록 B)과 이가온 — 지시 범위 밖이라 기본값 사무직 |

## 2026-09-01 — 사무직 / 현장직 직군 구분과 시급제

### 세 가지는 서로 다른 축입니다

- **직군** 사무직 / 현장직 — 어떤 성격의 일을 하는가
- **고용형태** 정규직 / 계약직 / 단시간 / 인턴 — 어떤 신분으로 고용되었는가
- **급여지급방식** 월급 / 연봉 / 시급 / 일급 — 임금을 어떤 단위로 지급하는가

직군을 고용형태로 대신할 수 없습니다. 정규직 현장직과 정규직 사무직은 고용형태가 같지만
급여 계산과 근태 관리가 다릅니다. 파나시아 구 ERP에도 `HU067 부서그룹`(사무관리직/생산현장직),
`HU024 사원구분`, `HU028 급여지급방식`이 각각 별도 코드군으로 있었습니다.

### 급여지급방식을 담을 자리가 아예 없었습니다

P4에서 만든 계산 엔진은 시급·일급을 계산할 수 있었지만, `employees`에는 **급여지급방식
컬럼 자체가 없어** 전원이 월급제로 계산되고 있었습니다. 현장직을 넣을 수 없는 상태였습니다.

`drizzle/0011_add_job_class_and_pay_method.sql` — `job_class`, `pay_method`, `hourly_wage`

`hourly_wage`를 따로 둔 이유는, 기본급 컬럼 하나에 방식에 따라 다른 의미의 금액(월급 300만 /
시급 1만 3천)을 담으면 "이 숫자가 무엇인지"를 화면마다 다시 판단해야 하고 급여에서 그런
모호함은 곧 사고이기 때문입니다.

### 현장직 → 시급제 기본값

직군을 현장직으로 바꾸면 급여방식이 시급제로 **따라옵니다.** 강제는 아닙니다 —
"일반적으로" 그렇다는 것이므로 담당자가 다시 고를 수 있고, **이미 기본값과 다르게 지정해 둔
사람은 건드리지 않습니다.** 의도적으로 정한 값을 조용히 덮으면 안 됩니다. 바뀐 경우에는
안내 문구로 알립니다.

인력대장에 **직군**(기본 프리셋) · **급여방식** · **시급·일급** 열과 **현장직 프리셋**을
추가했습니다. 사원 등록 폼도 급여방식에 따라 "기본급(월)" 또는 "시급/일급"으로 바뀝니다.

### 버그 수정

- **시급직 기본급이 20% 부풀려졌습니다.** 1일 근로시간을 `209 ÷ 21.75 ≈ 9.6시간`으로
  잡았는데, 209시간에는 **주휴시간이 포함**되어 있어 실근로시간이 아닙니다. 기준값에
  `standardDailyHours: 8`을 명시하고 그 값을 쓰도록 고쳤습니다. 시급 13,000원 기준으로
  1인당 월 46만 원가량 과다 계산되던 문제입니다.
- **저장된 기준값에 새 항목이 없으면 계산이 NaN이 되었습니다.** 기준값은 JSON 한 덩어리로
  저장되므로 항목이 추가되면 기존 연도 JSON에는 그 항목이 없습니다. 조회 시 항상 기본값
  위에 덮어씌우도록 했습니다 — 급여에서 조용한 NaN은 지급까지 갈 수 있습니다.
- **프리셋이 열 순서를 정하지 못했습니다.** 표시 여부만 바꾸다 보니 현장직 프리셋에서
  시급 열이 상태 뒤로 밀렸습니다. 프리셋이 순서까지 정하도록 했습니다.

### 아직 다루지 않은 것

**주휴수당** — 주 15시간 이상 근로자에게 법정 지급 대상이지만, 주 단위 근태 패턴을 봐야
정확히 계산되고 회사의 지급 방식(포괄 여부)에 따라 달라집니다. 지금은 계산하지 않으므로
현장직 급여에 별도 항목으로 반영해야 합니다.

## 2026-09-01 — 중소기업용 개편 4단계 (급여 계산 · 급여대장)

### 설정한 요율이 계산에 반영되지 않고 있었습니다

개편 전 `calculateInsurance()`는 `INSURANCE_RATES` 상수를 썼고, `설정 > 급여`의
국민연금·건강보험·장기요양·고용보험 요율과 비과세 한도는 **화면에 표시만 되고 계산에
닿지 않았습니다.** 부양가족 수도 화면 입력과 무관하게 1로 고정, 식대·교통비도 한도 설정과
무관하게 200,000원 고정이었습니다. 값이 우연히 같아서 드러나지 않았을 뿐, 담당자가 요율을
고쳐도 급여는 그대로 나오는 상태였습니다.

요율을 못 쓰는 것보다 **쓸 수 있다고 보이면서 안 쓰이는 쪽이 더 위험**하므로, 이번 단계의
첫 작업으로 잡았습니다.

### 급여 계산 엔진 (신규)

| 파일 | 내용 |
|------|------|
| `src/lib/payroll/rate-set.ts` | 해마다 바뀌는 숫자 한 덩어리 (요율·비과세 한도·근로소득공제·세율 구간·가산율) |
| `src/lib/payroll/engine.ts` | 순수 계산 함수. DB·React를 모릅니다 |
| `drizzle/0010_add_payroll_rate_sets.sql` | `payroll_rate_sets` — 연도별 기준값 |
| `src/lib/actions/payroll-rate-actions.ts` | 조회·저장·다음 해 복사 |

- 해당 연도 값이 없으면 **가장 가까운 과거 연도 값을 이어 쓰고**, 어느 값을 썼는지
  (`exact` / `carried` / `default`)를 함께 돌려줍니다. 새해 첫 급여가 기준값 미등록으로
  막히면 안 되지만, 전년도 요율로 돌고 있다는 사실은 드러나야 하기 때문입니다.
- 기존 `insurance.ts` · `korean-tax.ts`는 엔진에 위임하는 껍데기로 남겼습니다. 구현이 두
  벌이면 화면마다 다른 금액이 나옵니다.

### 계산이 다루는 것

- **급여지급방식** — 월급 · 연봉 · 시급 · 일급
- **중도 입퇴사 일할계산** — 소정근로일수 대비 재직일수
- **비과세 한도** — 한도까지는 비과세 줄, 초과분은 과세 줄로 **분리**. 한 줄에 섞으면
  명세서에서 과세소득이 왜 그 금액인지 설명할 수 없습니다
- **국민연금 상·하한**, 부양가족 수에 따른 인적공제
- 모든 줄에 계산식 문자열이 붙습니다

검증(스크립트): 표준 월급제 합계 검산, 식대 30만 원의 20만/10만 분리, 소정 22일 중 10일
일할계산, 시급제, 연금 상한, **요율을 바꾸면 결과가 바뀌는지**(기존 버그의 회귀 검사),
부양가족 수 반영, 과세소득 0원일 때 공제 0원.

### 급여대장

요약 8열 목록을 **항목 전개형 가로 대장**으로 교체했습니다. 회계 검토나 노무 자문에 넘길 때
필요한 건 실수령액 합계가 아니라 항목별 금액이고, 요약본만 있으면 결국 담당자가 엑셀에서
다시 만듭니다.

- 열은 급여항목 마스터가 아니라 **그 달에 실제로 쓰인 항목**에서 뽑습니다
- 부서별 소계 · 총계, 엑셀·CSV 내보내기
- **전월 대비 실수령액 ±10% 초과**와 신규 지급 건을 표시하고 그 건만 추려 볼 수 있습니다
- **은행 이체파일** — 계좌 미등록자는 파일에서 빼고 누구인지 알려 줍니다. 조용히 빠지면
  그 사람만 급여를 못 받습니다
- 선택 후 일괄 확정 / 지급완료 / 삭제 (삭제는 작성중 건만)

### 버그 수정

- **그리드가 늦게 도착한 열을 통째로 버렸습니다.** 열 구성이 `order` 배열 기준인데
  재조정이 마운트 시점에만 돌아, 급여대장처럼 데이터가 온 뒤에 열이 정해지는 화면은
  항목 열이 아예 나오지 않았습니다. 열 집합이 바뀔 때마다 재조정하도록 고쳤습니다.
- 같은 맥락에서 **열 순서를 정의 순서로** 잡도록 바꿨습니다. 저장된 순서를 그대로 따르면
  나중에 생긴 지급 항목 열이 합계 뒤로 밀립니다. 드래그 재정렬 기능이 아직 없어 저장된
  순서에는 사용자 의도가 담겨 있지 않습니다.
- **과세소득이 0원인데 국민연금이 부과**되었습니다. 기준소득 하한을 그대로 적용한 탓으로,
  무급휴직이나 급여가 전부 비과세인 달에 하한액 기준 보험료가 찍혔습니다.
- 비율 표기 정규식이 자릿수를 하나 더 지워 `4.5%`가 `4.%`로 나왔습니다.

## 2026-09-01 — 화면 디자인을 국내 인사 제품 톤으로 정리

다우오피스 인사관리 화면을 참조해 디자인 토큰과 화면 문법을 다시 잡았습니다. 국내
인사담당자가 이미 익숙한 화면 톤을 따라가면 학습 비용이 줄어들기 때문입니다. **로고·상호·
브랜드 그래픽은 가져오지 않았고** 대한오토텍 브랜딩을 그대로 씁니다.

### 색

제품 화면 세 장(근태 현황 · 내 인사정보 · 법정의무교육 목록)에서 픽셀을 추출해 잡았습니다.

| 용도 | 값 |
|------|-----|
| 페이지 바탕 | `#F8F8F8` — 이전에는 순백이라 카드가 배경과 구분되지 않았습니다 |
| 카드 | `#FFFFFF` |
| 헤어라인 | `#E8E8E8` · 입력 테두리 `#D8D8D8` |
| 본문 / 보조 | `#202020` / `#909090` |
| 액션 | `#08A8C0` 틸시안 — 세 화면에 모두 등장하는 유일한 공통 강조색 |
| 상태·진행률 | 민트 `#48D0A8` · 소프트블루, 바이올렛은 브랜드 전용 |

이전 인디고(`oklch(0.55 0.2 260)`)를 대체합니다.

### 형태·밀도

- 반경 10px → **8px**(카드) / 6px(컨트롤)
- 그림자를 걷어내고 헤어라인 한 줄로 구분. 선택 상태는 채움이 아니라 **테두리를 진하게**
- 표: 세로선 제거, 가로 헤어라인만. 글자 13px → **12px**, 헤더 11px 회색
- 버튼 기본 높이 36px → **32px**(`size="sm"`)

### 화면 문법

- `PageHeader` 신설 — 제목(20px) + ⓘ 도움말 한 줄 + 우측 액션. 메뉴 이름만 보고 화면
  용도를 짐작해야 하는 상황을 없애기 위한 것으로, 인력대장·입퇴사 진행에 적용했습니다.
- `StatsCard` 재작성 — 라벨 위, 숫자 크게, **단위를 작고 흐리게**(57 명). 숫자를 훑을 때
  단위가 시선을 뺏지 않습니다. 클릭 가능한 카드와 선택 상태를 지원합니다.
- 좌측 메뉴 — 활성 항목을 왼쪽 색 막대에서 **옅은 회색 면**으로. 막대는 화면 왼쪽 끝에
  세로선을 하나 더 그어 메뉴가 늘어날수록 지저분해집니다.

### 참조하지 않은 것

다우오피스 사원카드는 탭 13개(기본·신상·인사발령·경력·포상/징계 …)로 나뉩니다. P2에서
좌측 요약 패널 + 섹션 스크롤로 바꾼 구조를 유지했습니다 — 인사 업무를 처음 맡은 사람에게는
탭을 오가며 정보를 재구성하는 쪽이 더 어렵기 때문입니다.

2단 GNB(아이콘 레일 + 모듈 패널)도 보류했습니다. 메뉴 재편(5개로 축소)과 함께 결정할
사안이라, 지금 바꾸면 두 번 흔들립니다.

## 2026-09-01 — 중소기업용 개편 3단계 (입퇴사 파이프라인)

입퇴사가 잦은 회사에서 비용이 큰 쪽은 계산이 아니라 **누락**입니다. 취득신고를 빠뜨리고,
연차 정산을 놓치고, 계정을 살려 둡니다. 그래서 사람이 기억해서 시작하는 구조를 없앴습니다.

### 새 체크리스트를 만들지 않은 이유

이미 워크플로우 엔진(템플릿 · 단계 · 태스크 · 제출서류 · 진행률)이 완성되어 있었습니다.
빠져 있던 것은 엔진이 아니라 **연결**이었습니다 — 담당자가 직접 템플릿을 고르고 대상자를
지정해야 프로세스가 열렸습니다. 체크리스트를 새로 만들면 항목을 두 군데서 관리하게 되고
곧 서로 어긋나므로, 기존 엔진에 입퇴사 사건을 물렸습니다.

| 파일 | 내용 |
|------|------|
| `src/lib/actions/pipeline-actions.ts` | 프로세스 자동 착수, 일괄 입사·퇴사, 홈 요약 |
| `src/components/employee/pipeline-board.tsx` | 입퇴사 진행 그리드 |
| `src/components/employee/bulk-hire-dialog.tsx` | 일괄 입사등록 (붙여넣기 → 검증 → 자동 채번) |
| `src/components/employee/bulk-resign-dialog.tsx` | 일괄 퇴사처리 (정산 미리보기 → 확정) |
| `src/components/dashboard/todo-panel.tsx` | 홈 "이번 주 할 일" |

### 동작

- **사원 등록 → 입사 프로세스 자동 착수.** 같은 유형이 이미 열려 있으면 만들지 않습니다.
- **퇴사 처리 → 퇴사 프로세스 자동 착수** + 재직 상태 변경 + 소속 이력을 퇴사일에서 닫기.
- **입퇴사 진행** (`/employees/pipeline`) — 사람 단위로 진행률과 **다음 할 일**을 보여 주고,
  그 자리에서 완료 처리합니다. 착수 후 14일이 지나면 지연으로 표시합니다.
- **일괄 입사등록** — 엑셀 블록을 붙여넣으면 사번을 자동 채번하고, 행마다 프로세스를 엽니다.
  부서·직급·직책·고용형태는 한글 이름으로 씁니다. 오류 행은 사유와 함께 제외됩니다.
- **일괄 퇴사처리** — 인력대장에서 여러 명을 골라 실행합니다. 확정 전에 근속일수, 퇴직금
  대상 여부(1년 이상), 잔여 연차를 명단으로 보여 줍니다.
- **홈 "이번 주 할 일"** — 입사·퇴사·계약만료·예정발령·휴가승인·결재 대기를 건수로 묶고,
  0건인 항목은 숨깁니다.

### 버그 수정

- 퇴사 처리 시 끝나지 않은 입사 프로세스가 그대로 남아, 퇴사자의 입사 항목이 "이번 주 할 일"에
  영구히 집계되었습니다. 퇴사 확정 시 진행 중인 입사 프로세스를 취소하도록 했습니다.

### 4대보험에 관하여

신고 **화면**은 범위에서 제외했지만, 프로세스 템플릿의 "4대보험 취득신고 / 상실신고"
체크 항목은 남겼습니다. 시스템 밖(4대보험 정보연계센터)에서 처리하더라도 빠뜨리면 안 되는
일이라, 지우면 이 단계의 목적인 누락 방지에 어긋납니다.

## 2026-09-01 — 중소기업용 개편 2단계 (소속 이력 · 인력대장 심화)

### 소속 이력 (신규 · 구조 변경)

`employees`에는 현재 부서·직급·직책만 있었습니다. 오늘 조직도를 그리는 데는 충분하지만
"3월에는 어디였나", "그때 직급으로 급여를 다시 계산", "경력증명서" 같은 요구에는 전혀 답을
주지 못합니다. 그래서 **기간(구간)** 을 기록으로 삼고, `employees`의 컬럼은 오늘 유효한
구간의 사본으로 격하했습니다.

| 파일 | 내용 |
|------|------|
| `drizzle/0009_add_employee_assignments.sql` | `employee_assignments` 테이블 + 재직자 전원 소급 생성 |
| `src/lib/actions/assignment-actions.ts` | 구간 개폐, 시점 조회(as-of), 현재값 동기화 |
| `src/components/employee/employee-assignment-history.tsx` | 사원카드의 소속 이력 타임라인 |

- 한 직원의 구간은 겹치지 않습니다. 새 구간을 열면 직전 구간이 그 전날로 닫힙니다.
- **미래 발령은 기록만 하고 오늘의 인사정보를 바꾸지 않습니다.** 발령일이 지나면
  `syncCurrentAssignments()`가 옮겨 담습니다. 자료를 읽을 때마다 한 번 맞추는 방식이라
  온프레미스 설치에 크론을 따로 걸 필요가 없습니다.
- 인사발령을 등록하면 구간이 함께 열리고, 발령을 지우면 구간도 걷히고 직전 구간이 다시
  열립니다. 사원 등록 시에는 입사일부터 시작하는 최초 구간이 생깁니다.

검증: 김동현(0002)에게 과거 발령(2026-03-01 생산팀)과 미래 발령(2027-01-01 기술연구팀·과장)을
넣었을 때 구간이 `~2026-02-28 / 2026-03-01~2026-12-31 / 2027-01-01~`로 끊기고, 인사정보
현재값은 생산팀에 머물며, 시점 조회가 2026-02-01→미배정, 2026-06-15→생산팀,
2027-06-01→기술연구팀을 돌려주는 것을 확인했습니다.

### 인력대장

- **부서 트리 필터** — 상위 부서를 고르면 하위 부서까지 함께 걸립니다. 부서별 인원수는
  지금 보고 있는 재직 상태 기준으로 표시됩니다.
- **노동자명부** (`/employees/roster`) — 근로기준법 제41조 법정 서식. 기재사항이 고정되어
  있어 열을 바꿀 수 없고, 재직·휴직 / 퇴직자 포함을 골라 인쇄합니다.

### 사원카드

탭 7개를 좌측 요약 패널 + 우측 섹션 스크롤로 바꿨습니다. 사람을 식별하는 정보(사진·성명·
소속·연락처)가 늘 화면에 남고, 좌측 목차는 스크롤 위치를 따라갑니다. 소속 이력 섹션이
새로 들어갔습니다.

### 조직관리

`/organization`이 조직도와 기준정보(부서·직급·직책·직무·호봉)를 함께 다룹니다. 설정에서
그 5개 탭을 뺐습니다 — 24탭 → 18탭.

### 버그 수정

- 소속 이력에서 "현재" 배지가 종료일 없는 구간에만 붙어, 뒤에 예정 발령이 걸린 사람은
  현재 구간이 과거로 표시됐습니다. 오늘을 덮는 구간을 현재로 보도록 고쳤습니다.

## 2026-09-01 — 중소기업용 개편 1단계 (그리드 플랫폼)

인사 업무를 잘 모르는 담당자도 쓸 수 있도록 화면을 정리하고, 모든 대장 화면의 토대가 될
그리드를 구축했습니다.

### 그리드 플랫폼 (신규)

| 파일 | 내용 |
|------|------|
| `src/components/grid/data-grid.tsx` | 공통 `<DataGrid>` — 열 고정·정렬·필터·인라인 편집·소계·가상화 |
| `src/components/grid/types.ts` | 열 정의(`GridColumn`)와 저장 구성(`GridViewState`) |
| `src/components/grid/use-grid-view.ts` | 열 구성 상태 + localStorage 복원 + 열 집합 재조정 |
| `src/components/grid/grid-toolbar.tsx` | 검색·열 선택·밀도·저장된 구성·내보내기 |
| `src/components/grid/grid-paste.ts` | 엑셀 클립보드(TSV) 파서와 셀 단위 검증 |
| `src/components/grid/grid-paste-dialog.tsx` | 붙여넣기 검토 창 — 정상 행만 저장 |
| `src/components/grid/grid-format.ts` | 값 표시·정렬 비교 |
| `src/lib/actions/grid-export-actions.ts` | ExcelJS 서버 생성 (틀고정·자동필터·숫자서식·합계행) |
| `src/lib/actions/grid-view-actions.ts` | 저장된 화면 구성 CRUD |
| `drizzle/0008_add_grid_views.sql` | `grid_views` 테이블 |

### 인력대장

- `/employees`를 8열·10행 페이지네이션 표에서 그리드 기반 **인력대장**으로 교체
- 재직/휴직/퇴직/전체 탭, 열 구성 프리셋 6종(기본·연락처·급여·계약수습·입퇴사·노동자명부)
- 부서·직급·직책·고용형태·입사일·상태 등을 셀에서 바로 수정, 엑셀에서 붙여넣기
- 부서별 소계, 엑셀·CSV 내보내기

### 버그 수정

- **목록 순서가 불안정했습니다.** `fetchEmployeeData()`의 모든 조회에 `ORDER BY`가 없어
  PostgreSQL 힙 순서로 반환되었고, 행을 수정하면 그 행이 결과 맨 끝으로 이동했습니다.
  인라인 편집 그리드에서는 방금 고친 행이 화면 밖으로 사라지는 문제가 되어, 사번·레벨·정렬
  순서 기준의 명시적 정렬을 추가했습니다.
- 편집 가능한 셀에서 행 클릭 이동과 더블클릭 편집이 충돌해 편집기를 열 수 없었습니다.
  행 클릭 이동을 없애고 행 왼쪽의 ↗ 버튼으로 상세를 열도록 바꿨습니다.
- 부서별 소계·합계 라벨이 첫 열 너비에 잘렸습니다. 소계가 시작되는 열 앞까지 라벨이
  `grid-column: span`으로 펼쳐지도록 수정했습니다.

### 회사 정보

- 파나시아 관련 정보(회사명·사업자번호·조직 시드·CHANGELOG·문서)를 모두 제거
- 대한오토텍(주) / 621-81-98896 기준으로 기본값·브랜딩 교체
- 초기 인력 명부 55명 적재 (`src/lib/demo-data/employee-seed.ts`) — 확보된 값은 성명과 회사
  이메일뿐이라 부서·직급·직책·입사일은 자리표시 값이며, 인력대장에서 교체하는 것을 전제로 함

### 범위에서 제외한 모듈

출장비 정산 · 채용관리 · 교육관리 · 인사평가 · 전자계약 · 4대보험 신고를 라우트·메뉴·설정 탭·
스토어·서버 액션·시드·DB 테이블까지 제거했습니다 (`drizzle/0007_drop_talent_and_trip_expense.sql`).

### 화면

- 다크모드 제거 — `next-themes` 의존성, `.dark` 토큰 세트, `dark:` 유틸리티 157개를 걷어내고
  흰 바탕 단일 테마로 고정. 설정의 테마 선택 항목도 삭제
- 사이드바 메뉴 15개 → 11개
