# i18n (한/영 옵션) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** HRMS의 크롬 영역(사이드바, 헤더, 알림벨)에 한글/영문 토글 기능을 추가하고, 향후 페이지 본문 번역 확장의 기반이 되는 i18n 인프라를 구축한다.

**Architecture:** 외부 i18n 라이브러리를 도입하지 않고 React hook(`useT`) + 평탄화된 dictionary(`ko.ts`, `en.ts`) 조합으로 자체 구현한다. Locale은 기존 `settings-store.display`에 추가하여 Zustand persist를 그대로 활용하고, 헤더 우측 드롭다운에서 전환한다. 영문 사전 누락 시 한글로 폴백하고, dev에서 콘솔 경고를 띄운다.

**Tech Stack:** Next.js 16 (App Router) / React 19 / TypeScript / Zustand 5 (+ persist) / shadcn-ui (Radix) / lucide-react / tailwindcss 4.

**테스트 환경:** 프로젝트에 자동 테스트 프레임워크가 없으므로, 각 Task는 다음 두 가지로 검증한다:
- `npm run lint`와 `npm run build`로 타입/린트 오류 확인 (자동)
- `npm run dev` 후 브라우저에서 명시된 시나리오 확인 (수동)

---

## File Structure

신규 (5):
- `src/lib/i18n/dictionaries/ko.ts` — 한글 사전 (single source of truth, 키셋의 기준)
- `src/lib/i18n/dictionaries/en.ts` — 영문 사전 (`ko`의 키셋과 동일하도록 타입 강제)
- `src/lib/i18n/types.ts` — `Locale`, `TranslationKey`, `Dictionary` 타입
- `src/lib/i18n/use-translation.ts` — `useT()` hook, interpolation 지원
- `src/components/layout/language-toggle.tsx` — 헤더용 언어 전환 드롭다운

수정 (6):
- `src/lib/stores/settings-store.ts` — `DisplayState.locale` 추가, version 6→7 마이그레이션
- `src/lib/constants/menu-items.ts` — `label`/`group`/`description`을 번역 키로 변환
- `src/components/layout/sidebar.tsx` — `t(item.label)`, `t(group.label)` 적용
- `src/components/layout/header.tsx` — 검색/내 정보/로그아웃/역할 라벨 i18n + `<LanguageToggle/>` 삽입
- `src/components/layout/display-settings-applier.tsx` — `document.documentElement.lang` 동기화
- `src/components/layout/notification-bell.tsx` — 알림 칭크롬 라벨 i18n (알림, X개 미확인, 모두 읽음, 알림 없음, timeAgo)

---

### Task 1: i18n 타입 정의

**Files:**
- Create: `src/lib/i18n/types.ts`

- [ ] **Step 1: 타입 파일 작성**

```typescript
// src/lib/i18n/types.ts
import type { ko } from './dictionaries/ko';

export type Locale = 'ko' | 'en';
export type TranslationKey = keyof typeof ko;
export type Dictionary = Record<TranslationKey, string>;
```

- [ ] **Step 2: 임시로 컴파일 가능하도록 빈 사전 placeholder 생성 (Task 2에서 채움)**

```typescript
// src/lib/i18n/dictionaries/ko.ts — 임시 placeholder (Task 2에서 교체)
export const ko = {} as const;
```

- [ ] **Step 3: lint 통과 확인**

```bash
npx eslint src/lib/i18n/types.ts
```
Expected: 오류 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/lib/i18n/types.ts src/lib/i18n/dictionaries/ko.ts
git commit -m "feat(i18n): add types and placeholder ko dictionary"
```

---

### Task 2: 한글 사전 작성

**Files:**
- Modify: `src/lib/i18n/dictionaries/ko.ts` (전체 교체)

- [ ] **Step 1: 한글 사전 작성 (전체 키셋 정의 — 영문 사전의 기준)**

```typescript
// src/lib/i18n/dictionaries/ko.ts
export const ko = {
  // 공통 UI
  'common.save': '저장',
  'common.cancel': '취소',
  'common.edit': '수정',
  'common.delete': '삭제',
  'common.search': '검색',
  'common.next': '다음',
  'common.prev': '이전',
  'common.confirm': '확인',
  'common.close': '닫기',
  'common.required': '필수',
  'common.loading': '불러오는 중...',
  'common.noData': '데이터가 없습니다',
  'common.searchPlaceholder': '검색...',

  // 메뉴 라벨
  'menu.dashboard': '대시보드',
  'menu.myPage': '마이페이지',
  'menu.organization': '조직도',
  'menu.employees': '인사정보',
  'menu.appointments': '인사발령',
  'menu.attendance': '근태관리',
  'menu.leave': '휴가관리',
  'menu.payroll': '급여관리',
  'menu.training': '교육관리',
  'menu.approval': '전자결재',
  'menu.contracts': '전자계약',
  'menu.auditLog': '감사로그',
  'menu.settings': '설정',

  // 메뉴 설명 (command palette subtitle)
  'menuDesc.dashboard': '메인 대시보드',
  'menuDesc.myPage': '내 정보 관리',
  'menuDesc.organization': '조직 구조 관리',
  'menuDesc.employees': '사원 정보 관리',
  'menuDesc.appointments': '인사발령 관리',
  'menuDesc.attendance': '출퇴근/근태 관리',
  'menuDesc.leave': '휴가 신청/관리',
  'menuDesc.payroll': '급여 계산/관리',
  'menuDesc.training': '교육 과정/이수 관리',
  'menuDesc.approval': '전자결재 시스템',
  'menuDesc.contracts': '근로계약서 관리',
  'menuDesc.auditLog': '시스템 감사 로그',
  'menuDesc.settings': '시스템 설정',

  // 메뉴 그룹
  'menuGroup.home': '홈',
  'menuGroup.hr': '인사관리',
  'menuGroup.work': '근무관리',
  'menuGroup.payroll': '급여관리',
  'menuGroup.talent': '인재개발',
  'menuGroup.support': '업무지원',
  'menuGroup.system': '시스템',

  // 헤더
  'header.myInfo': '내 정보',
  'header.logout': '로그아웃',
  'header.kbdHint': '⌘K',

  // Command Palette
  'commandPalette.title': '글로벌 검색',
  'commandPalette.description': '직원, 메뉴를 검색합니다',
  'commandPalette.inputPlaceholder': '검색어를 입력하세요... (직원, 메뉴)',
  'commandPalette.empty': '검색 결과가 없습니다.',
  'commandPalette.groupEmployees': '직원',
  'commandPalette.groupMenus': '메뉴',

  // 언어 전환
  'language.korean': '한국어',
  'language.english': 'English',
  'language.label.ko': '한',
  'language.label.en': 'EN',

  // 역할
  'role.admin': '시스템관리자',
  'role.hr_manager': '인사담당자',
  'role.dept_manager': '부서관리자',
  'role.employee': '일반사원',

  // 알림 (notification bell)
  'notification.title': '알림',
  'notification.unreadBadge': '{{count}}개 미확인',
  'notification.markAllRead': '모두 읽음',
  'notification.clearAllConfirm': '모든 알림을 삭제하시겠습니까?',
  'notification.empty': '알림이 없습니다.',
  'notification.timeAgo.justNow': '방금',
  'notification.timeAgo.minutes': '{{count}}분 전',
  'notification.timeAgo.hours': '{{count}}시간 전',
  'notification.timeAgo.days': '{{count}}일 전',

  // 공통 토스트
  'toast.saved': '저장되었습니다',
  'toast.deleted': '삭제되었습니다',
  'toast.updated': '수정되었습니다',
  'toast.error': '오류가 발생했습니다',
  'toast.networkError': '네트워크 오류가 발생했습니다',
  'toast.requiredField': '필수 항목을 입력해주세요',
} as const;
```

- [ ] **Step 2: 타입 체크 통과 확인**

```bash
npx tsc --noEmit
```
Expected: 오류 없음 (en.ts는 아직 placeholder 상태이므로 다음 Task에서 채움).

- [ ] **Step 3: 커밋**

```bash
git add src/lib/i18n/dictionaries/ko.ts
git commit -m "feat(i18n): add Korean dictionary (full keyset)"
```

---

### Task 3: 영문 사전 작성

**Files:**
- Create: `src/lib/i18n/dictionaries/en.ts`

- [ ] **Step 1: 영문 사전 작성 — `ko` 키셋과 동일하게 타입 강제**

```typescript
// src/lib/i18n/dictionaries/en.ts
import type { ko } from './ko';

export const en: Record<keyof typeof ko, string> = {
  // Common UI
  'common.save': 'Save',
  'common.cancel': 'Cancel',
  'common.edit': 'Edit',
  'common.delete': 'Delete',
  'common.search': 'Search',
  'common.next': 'Next',
  'common.prev': 'Previous',
  'common.confirm': 'Confirm',
  'common.close': 'Close',
  'common.required': 'Required',
  'common.loading': 'Loading...',
  'common.noData': 'No data',
  'common.searchPlaceholder': 'Search...',

  // Menu labels
  'menu.dashboard': 'Dashboard',
  'menu.myPage': 'My Page',
  'menu.organization': 'Organization',
  'menu.employees': 'Employees',
  'menu.appointments': 'Appointments',
  'menu.attendance': 'Attendance',
  'menu.leave': 'Leave',
  'menu.payroll': 'Payroll',
  'menu.training': 'Training',
  'menu.approval': 'Approvals',
  'menu.contracts': 'Contracts',
  'menu.auditLog': 'Audit Log',
  'menu.settings': 'Settings',

  // Menu descriptions
  'menuDesc.dashboard': 'Main dashboard',
  'menuDesc.myPage': 'Manage my profile',
  'menuDesc.organization': 'Organization structure',
  'menuDesc.employees': 'Employee records',
  'menuDesc.appointments': 'HR appointments',
  'menuDesc.attendance': 'Attendance management',
  'menuDesc.leave': 'Leave requests',
  'menuDesc.payroll': 'Payroll management',
  'menuDesc.training': 'Training programs',
  'menuDesc.approval': 'Electronic approval',
  'menuDesc.contracts': 'Employment contracts',
  'menuDesc.auditLog': 'System audit log',
  'menuDesc.settings': 'System settings',

  // Menu groups
  'menuGroup.home': 'Home',
  'menuGroup.hr': 'HR',
  'menuGroup.work': 'Work',
  'menuGroup.payroll': 'Payroll',
  'menuGroup.talent': 'Talent',
  'menuGroup.support': 'Support',
  'menuGroup.system': 'System',

  // Header
  'header.myInfo': 'My Info',
  'header.logout': 'Logout',
  'header.kbdHint': '⌘K',

  // Command Palette
  'commandPalette.title': 'Global Search',
  'commandPalette.description': 'Search employees and menus',
  'commandPalette.inputPlaceholder': 'Type to search... (employees, menus)',
  'commandPalette.empty': 'No results.',
  'commandPalette.groupEmployees': 'Employees',
  'commandPalette.groupMenus': 'Menus',

  // Language
  'language.korean': '한국어',
  'language.english': 'English',
  'language.label.ko': '한',
  'language.label.en': 'EN',

  // Roles
  'role.admin': 'System Admin',
  'role.hr_manager': 'HR Manager',
  'role.dept_manager': 'Department Manager',
  'role.employee': 'Employee',

  // Notifications
  'notification.title': 'Notifications',
  'notification.unreadBadge': '{{count}} unread',
  'notification.markAllRead': 'Mark all read',
  'notification.clearAllConfirm': 'Delete all notifications?',
  'notification.empty': 'No notifications.',
  'notification.timeAgo.justNow': 'just now',
  'notification.timeAgo.minutes': '{{count}}m ago',
  'notification.timeAgo.hours': '{{count}}h ago',
  'notification.timeAgo.days': '{{count}}d ago',

  // Toasts
  'toast.saved': 'Saved.',
  'toast.deleted': 'Deleted.',
  'toast.updated': 'Updated.',
  'toast.error': 'An error occurred.',
  'toast.networkError': 'Network error.',
  'toast.requiredField': 'Please fill in required fields.',
};
```

- [ ] **Step 2: 타입 체크 통과 확인 (키 누락 시 컴파일 에러)**

```bash
npx tsc --noEmit
```
Expected: 오류 없음. 만약 키 누락 시 `Type 'X' is missing the following properties from type 'Record<keyof typeof ko, string>'` 발생 — 누락된 키 추가.

- [ ] **Step 3: 커밋**

```bash
git add src/lib/i18n/dictionaries/en.ts
git commit -m "feat(i18n): add English dictionary"
```

---

### Task 4: useT() hook 구현

**Files:**
- Create: `src/lib/i18n/use-translation.ts`

- [ ] **Step 1: hook 구현 — 폴백/인터폴레이션 포함**

```typescript
// src/lib/i18n/use-translation.ts
'use client';

import { useSettingsStore } from '@/lib/stores/settings-store';
import { ko } from './dictionaries/ko';
import { en } from './dictionaries/en';
import type { Locale, TranslationKey, Dictionary } from './types';

const DICTIONARIES: Record<Locale, Dictionary> = { ko, en };

function interpolate(template: string, vars?: Record<string, string | number>): string {
  if (!vars) return template;
  let out = template;
  for (const [k, v] of Object.entries(vars)) {
    out = out.replace(new RegExp(`\\{\\{${k}\\}\\}`, 'g'), String(v));
  }
  return out;
}

export function useT() {
  const locale = useSettingsStore((s) => s.display.locale);
  const t = (key: TranslationKey, vars?: Record<string, string | number>): string => {
    const value = DICTIONARIES[locale][key] ?? DICTIONARIES.ko[key];
    if (value === undefined) {
      if (process.env.NODE_ENV === 'development') {
        // eslint-disable-next-line no-console
        console.warn(`[i18n] missing translation key: ${key}`);
      }
      return key;
    }
    return interpolate(value, vars);
  };
  return { t, locale };
}
```

> 주의: 이 단계에서 `s.display.locale` 참조는 settings-store 변경(Task 5) 전이라 TS 에러가 난다. Task 5에서 store에 `locale` 필드를 추가하면 해결된다. **이 Task에서 lint/tsc 검증은 생략**하고, Task 5 완료 후 한 번에 검증한다.

- [ ] **Step 2: 커밋 (Task 5와 함께 빌드 검증)**

```bash
git add src/lib/i18n/use-translation.ts
git commit -m "feat(i18n): add useT hook with fallback and interpolation"
```

---

### Task 5: settings-store에 locale 추가

**Files:**
- Modify: `src/lib/stores/settings-store.ts`

- [ ] **Step 1: import 추가 (`Locale` 타입)**

`settings-store.ts` 상단 import 블록에 다음 한 줄 추가:

```typescript
import type { Locale } from '@/lib/i18n/types';
```

- [ ] **Step 2: `DisplayState`에 `locale` 필드 추가**

`settings-store.ts:18-26` 블록의 `DisplayState` interface를 다음과 같이 변경 — 마지막 줄에 `locale` 추가:

```typescript
export interface DisplayState {
  theme: 'system' | 'light' | 'dark';
  font_size: 'small' | 'medium' | 'large';
  content_density: 'compact' | 'comfortable' | 'spacious';
  sidebar_compact: boolean;
  rows_per_page: number;
  date_format: 'yyyy-MM-dd' | 'yyyy.MM.dd' | 'yyyy년 MM월 dd일';
  number_format: 'comma' | 'plain';
  locale: Locale;
}
```

- [ ] **Step 3: 초기값에 `locale: 'ko'` 추가**

`settings-store.ts`의 `display: { ... }` 초기값 블록(약 542줄)에서 마지막 항목 뒤에 `locale: 'ko',` 추가:

```typescript
display: {
  theme: 'system',
  font_size: 'medium',
  content_density: 'comfortable',
  sidebar_compact: false,
  rows_per_page: 10,
  date_format: 'yyyy-MM-dd',
  number_format: 'comma',
  locale: 'ko',
},
```

- [ ] **Step 4: persist `version`을 7로 올리고 마이그레이션 추가**

파일 하단 `persist` 옵션의 `version: 6` 을 `version: 7` 로 바꾸고, `migrate` 함수의 마지막 `if (version < 6)` 블록 **뒤에** 다음 블록을 추가:

```typescript
if (version < 7) {
  const display = (state.display as Record<string, unknown> | undefined) ?? {};
  state = {
    ...state,
    display: { ...display, locale: display.locale ?? 'ko' },
  };
}
```

- [ ] **Step 5: 타입 체크 및 빌드 통과 (Task 4의 useT()까지 함께 검증)**

```bash
npx tsc --noEmit
```
Expected: 오류 없음.

```bash
npm run build
```
Expected: build success.

- [ ] **Step 6: 브라우저 수동 검증 (localStorage 마이그레이션)**

```bash
npm run dev
```
브라우저 DevTools → Application → Local Storage → `hrms-settings` 항목 열어서:
- `state.display.locale === 'ko'` 확인 (기존 사용자도 한글 기본값으로 마이그레이션됨)
- localStorage를 비우고 새로 진입해도 `display.locale === 'ko'` 인지 확인

- [ ] **Step 7: 커밋**

```bash
git add src/lib/stores/settings-store.ts
git commit -m "feat(i18n): add display.locale to settings-store with v7 migration"
```

---

### Task 6: menu-items.ts를 번역 키로 변환

**Files:**
- Modify: `src/lib/constants/menu-items.ts` (전체 교체)

- [ ] **Step 1: 메뉴 항목의 `label`/`description`/`group`을 번역 키로 변경하고, 그룹 순서도 키 기반으로 변경**

```typescript
// src/lib/constants/menu-items.ts
import {
  LayoutDashboard,
  UserCircle,
  Network,
  Users,
  Clock,
  CalendarDays,
  Banknote,
  ArrowRightLeft,
  FileCheck,
  GraduationCap,
  ShieldAlert,
  Settings,
  FileSignature,
} from 'lucide-react';
import type { TranslationKey } from '@/lib/i18n/types';

export interface MenuItem {
  href: string;
  /** translation key — resolve with useT() */
  label: TranslationKey;
  icon: typeof LayoutDashboard;
  /** translation key — resolve with useT() */
  description: TranslationKey;
  /** translation key (menuGroup.*) */
  group: TranslationKey;
}

export type MenuGroup = {
  /** translation key (menuGroup.*) */
  label: TranslationKey;
  items: MenuItem[];
};

export const ALL_MENU_ITEMS: MenuItem[] = [
  // Home
  { href: '/', label: 'menu.dashboard', icon: LayoutDashboard, description: 'menuDesc.dashboard', group: 'menuGroup.home' },
  { href: '/my', label: 'menu.myPage', icon: UserCircle, description: 'menuDesc.myPage', group: 'menuGroup.home' },
  // HR
  { href: '/organization', label: 'menu.organization', icon: Network, description: 'menuDesc.organization', group: 'menuGroup.hr' },
  { href: '/employees', label: 'menu.employees', icon: Users, description: 'menuDesc.employees', group: 'menuGroup.hr' },
  { href: '/appointments', label: 'menu.appointments', icon: ArrowRightLeft, description: 'menuDesc.appointments', group: 'menuGroup.hr' },
  // Work
  { href: '/attendance', label: 'menu.attendance', icon: Clock, description: 'menuDesc.attendance', group: 'menuGroup.work' },
  { href: '/leave', label: 'menu.leave', icon: CalendarDays, description: 'menuDesc.leave', group: 'menuGroup.work' },
  // Payroll
  { href: '/payroll', label: 'menu.payroll', icon: Banknote, description: 'menuDesc.payroll', group: 'menuGroup.payroll' },
  // Talent
  { href: '/training', label: 'menu.training', icon: GraduationCap, description: 'menuDesc.training', group: 'menuGroup.talent' },
  // Support
  { href: '/approval', label: 'menu.approval', icon: FileCheck, description: 'menuDesc.approval', group: 'menuGroup.support' },
  { href: '/contracts', label: 'menu.contracts', icon: FileSignature, description: 'menuDesc.contracts', group: 'menuGroup.support' },
  // System
  { href: '/audit-log', label: 'menu.auditLog', icon: ShieldAlert, description: 'menuDesc.auditLog', group: 'menuGroup.system' },
  { href: '/settings', label: 'menu.settings', icon: Settings, description: 'menuDesc.settings', group: 'menuGroup.system' },
];

const GROUP_ORDER: TranslationKey[] = [
  'menuGroup.home',
  'menuGroup.hr',
  'menuGroup.work',
  'menuGroup.payroll',
  'menuGroup.talent',
  'menuGroup.support',
  'menuGroup.system',
];

export const MENU_GROUPS: MenuGroup[] = (() => {
  const map = new Map<TranslationKey, MenuItem[]>();
  for (const item of ALL_MENU_ITEMS) {
    if (!map.has(item.group)) map.set(item.group, []);
    map.get(item.group)!.push(item);
  }
  return GROUP_ORDER.filter((g) => map.has(g)).map((g) => ({ label: g, items: map.get(g)! }));
})();
```

- [ ] **Step 2: 타입 체크 통과**

```bash
npx tsc --noEmit
```
Expected: 오류 없음. (이 시점에 sidebar/command-palette 등 consumers가 한글 라벨을 직접 출력하던 코드는 깨지지 않는다 — `label` 타입이 string에서 union으로 좁아지지만 React는 string으로 렌더만 함. 다만 번역 안 된 키 그대로가 화면에 보임.)

> 참고: 이 시점에 dev 서버에서는 키 문자열(`menu.dashboard` 등)이 그대로 표시될 수 있다. Task 7에서 sidebar를 `t()`로 감싸면 정상화된다.

- [ ] **Step 3: 커밋**

```bash
git add src/lib/constants/menu-items.ts
git commit -m "refactor(i18n): convert menu-items labels/groups to translation keys"
```

---

### Task 7: Sidebar에서 useT() 적용

**Files:**
- Modify: `src/components/layout/sidebar.tsx`

- [ ] **Step 1: import 추가 및 사용처 교체**

`sidebar.tsx`에 import 추가:

```typescript
import { useT } from '@/lib/i18n/use-translation';
```

`Sidebar` 컴포넌트 본문 상단 (다른 hook 호출 근처)에 hook 호출 추가:

```typescript
const { t } = useT();
```

`group.label` 출력 부분(약 36줄)을 다음과 같이 변경:

```tsx
<p className="px-3 mb-1 text-[11px] font-semibold uppercase tracking-wider text-muted-foreground/60">
  {t(group.label)}
</p>
```

`item.label` 출력 부분(약 56줄)을 다음과 같이 변경:

```tsx
<item.icon className="h-4 w-4" />
{t(item.label)}
```

- [ ] **Step 2: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/sidebar.tsx
```
Expected: 오류 없음.

- [ ] **Step 3: 브라우저 수동 검증**

```bash
npm run dev
```
브라우저에서 사이드바를 보고:
- 메뉴 그룹명 `홈/인사관리/근무관리/...`가 정상 표시됨 (한글)
- 메뉴 항목명 `대시보드/마이페이지/...`가 정상 표시됨
- DevTools Console에서 `[i18n] missing` 경고 없음

- [ ] **Step 4: 커밋**

```bash
git add src/components/layout/sidebar.tsx
git commit -m "feat(i18n): translate sidebar labels via useT()"
```

---

### Task 8: command-palette에서 번역 키 적용

**Files:**
- Modify: `src/components/layout/command-palette.tsx`

Task 6에서 `menu.label`/`menu.description`이 번역 키로 바뀌었으므로, command-palette가 이를 그대로 출력하면 raw 키(예: `menu.dashboard`)가 노출된다. 또한 command-palette 자체의 한글 라벨(다이얼로그 타이틀, placeholder, 그룹명 등)도 i18n화한다.

- [ ] **Step 1: import 추가**

`command-palette.tsx:1-17` 영역의 import 블록에 추가:

```typescript
import { useSettingsStore } from '@/lib/stores/settings-store';
import { useT } from '@/lib/i18n/use-translation';
```

- [ ] **Step 2: 컴포넌트 본문에 hook 호출 추가**

`CommandPalette` 함수 상단에 다음을 추가 (기존 `const [open, setOpen] = useState(false);` 위 또는 인접 위치):

```typescript
const { t } = useT();
const locale = useSettingsStore((s) => s.display.locale);
```

> `t`를 useMemo 의존성으로 쓰면 매 렌더마다 함수가 새로 만들어져 memo가 무효화되므로, `locale`을 의존성으로 쓴다.

- [ ] **Step 3: entries 생성 부분에서 메뉴 라벨/설명을 t()로 변환**

`command-palette.tsx`의 `entries` useMemo 내부, 메뉴 항목 push 블록을 다음과 같이 변경:

```typescript
// Menu items
for (const menu of ALL_MENU_ITEMS) {
  result.push({
    id: `menu-${menu.href}`,
    category: 'menu',
    title: t(menu.label),
    subtitle: t(menu.description),
    href: menu.href,
  });
}
```

그리고 `entries` useMemo의 deps 배열을 다음과 같이 변경:

```typescript
}, [employees, departments, positionRanks, locale]);
```

(`t` 대신 `locale`을 deps로 사용한다. locale이 바뀌면 useMemo가 다시 실행되어 새 번역된 entries가 만들어진다.)

- [ ] **Step 4: CommandDialog props (title, description) 및 input placeholder 번역**

`command-palette.tsx:166-178` 영역을 다음과 같이 변경:

```tsx
<CommandDialog
  open={open}
  onOpenChange={(v) => { setOpen(v); if (!v) setQuery(''); }}
  title={t('commandPalette.title')}
  description={t('commandPalette.description')}
  shouldFilter={false}
  showCloseButton={false}
>
  <CommandInput
    placeholder={t('commandPalette.inputPlaceholder')}
    value={query}
    onValueChange={setQuery}
  />
```

- [ ] **Step 5: CommandEmpty와 그룹 헤더 번역**

`command-palette.tsx:179-185` 영역을 다음과 같이 변경:

```tsx
<CommandList>
  {query.trim() && results.length === 0 && (
    <CommandEmpty>{t('commandPalette.empty')}</CommandEmpty>
  )}
  {renderGroup(t('commandPalette.groupEmployees'), employeeResults)}
  {renderGroup(t('commandPalette.groupMenus'), menuResults)}
</CommandList>
```

- [ ] **Step 6: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/command-palette.tsx
```
Expected: 오류 없음.

- [ ] **Step 7: 브라우저 수동 검증**

```bash
npm run dev
```
브라우저에서 `⌘K`로 command palette 열고:
- 입력란 placeholder가 `검색어를 입력하세요... (직원, 메뉴)` 정상 표시
- "대시"라고 입력 → 메뉴 그룹 헤더 `메뉴`, 항목 제목 `대시보드`, 부제 `메인 대시보드` 표시
- 결과가 없을 때 `검색 결과가 없습니다.` 표시
- DevTools Console에 `[i18n] missing` 경고 없음

> 이 시점에서 LanguageToggle은 아직 헤더에 없으므로(Task 10에서 추가), 영문 전환은 Task 10에서 통합 검증한다.

- [ ] **Step 8: 커밋**

```bash
git add src/components/layout/command-palette.tsx
git commit -m "feat(i18n): translate command palette (dialog, input, groups, entries)"
```

---

### Task 9: Header 라벨 i18n

**Files:**
- Modify: `src/components/layout/header.tsx`

- [ ] **Step 1: import 추가**

```typescript
import { useT } from '@/lib/i18n/use-translation';
```

- [ ] **Step 2: hook 호출 및 라벨 매핑 교체**

`header.tsx` `Header` 컴포넌트 본문 시작부에 hook 호출 추가:

```typescript
const { t } = useT();
```

`roleLabels` 객체를 다음과 같이 `t()` 기반으로 교체:

```typescript
const roleLabels: Record<string, string> = {
  admin: t('role.admin'),
  hr_manager: t('role.hr_manager'),
  dept_manager: t('role.dept_manager'),
  employee: t('role.employee'),
};
```

검색 버튼 텍스트(`<span>검색...</span>` → `<span>{t('common.searchPlaceholder')}</span>`).

`<DropdownMenuItem asChild>` 내부의 "내 정보" 텍스트를 `{t('header.myInfo')}` 로 변경.

"로그아웃" 텍스트를 `{t('header.logout')}` 로 변경.

- [ ] **Step 3: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/header.tsx
```
Expected: 오류 없음.

- [ ] **Step 4: 브라우저 수동 검증**

```bash
npm run dev
```
- 헤더의 검색 placeholder가 `검색...` 표시
- 사용자 메뉴 클릭 시 `내 정보`, `로그아웃` 한글 표시
- 사용자 카드의 역할 라벨(예: `시스템관리자`) 한글 표시

- [ ] **Step 5: 커밋**

```bash
git add src/components/layout/header.tsx
git commit -m "feat(i18n): translate header chrome (search, my info, logout, roles)"
```

---

### Task 10: LanguageToggle 컴포넌트 및 헤더 배치

**Files:**
- Create: `src/components/layout/language-toggle.tsx`
- Modify: `src/components/layout/header.tsx`

- [ ] **Step 1: LanguageToggle 컴포넌트 작성**

```tsx
// src/components/layout/language-toggle.tsx
'use client';

import { Languages } from 'lucide-react';
import { Button } from '@/components/ui/button';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';
import { useSettingsStore } from '@/lib/stores/settings-store';
import { useT } from '@/lib/i18n/use-translation';

export function LanguageToggle() {
  const { t, locale } = useT();
  const updateDisplay = useSettingsStore((s) => s.updateDisplay);

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="sm" className="gap-1.5 h-9 px-2.5">
          <Languages className="h-4 w-4" />
          <span className="text-xs font-medium">
            {locale === 'ko' ? t('language.label.ko') : t('language.label.en')}
          </span>
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem
          onClick={() => updateDisplay({ locale: 'ko' })}
          className={locale === 'ko' ? 'font-semibold' : ''}
        >
          {t('language.korean')}
        </DropdownMenuItem>
        <DropdownMenuItem
          onClick={() => updateDisplay({ locale: 'en' })}
          className={locale === 'en' ? 'font-semibold' : ''}
        >
          {t('language.english')}
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 2: Header에 LanguageToggle 삽입**

`src/components/layout/header.tsx`의 import에 추가:

```typescript
import { LanguageToggle } from '@/components/layout/language-toggle';
```

`<div className="flex items-center gap-4">` 내부의 `<NotificationBell />` **앞**에 다음 한 줄 삽입:

```tsx
<LanguageToggle />
<NotificationBell />
```

- [ ] **Step 3: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/language-toggle.tsx src/components/layout/header.tsx
```
Expected: 오류 없음.

- [ ] **Step 4: 브라우저 수동 검증 (핵심 동작)**

```bash
npm run dev
```
1. 헤더 우측에 `Languages` 아이콘 + `한` 라벨 버튼이 표시됨
2. 클릭 → 드롭다운에 `한국어` / `English` 두 항목, 현재 선택은 굵게
3. `English` 클릭 → 즉시:
   - 사이드바 메뉴/그룹명이 영문으로 (`Home, HR, Work, ... / Dashboard, My Page, ...`)
   - 헤더 검색 placeholder `Search...`
   - 사용자 메뉴 `My Info`, `Logout`
   - 사용자 카드 역할 라벨 영문 (`System Admin` 등)
   - LanguageToggle 라벨이 `EN`으로 변경
4. 페이지 새로고침 → 영문 유지됨 (persist 확인)
5. `한국어` 클릭 → 한글로 즉시 복귀

- [ ] **Step 5: 커밋**

```bash
git add src/components/layout/language-toggle.tsx src/components/layout/header.tsx
git commit -m "feat(i18n): add LanguageToggle in header"
```

---

### Task 11: <html lang> 동기화

**Files:**
- Modify: `src/components/layout/display-settings-applier.tsx`

- [ ] **Step 1: locale에 따라 `document.documentElement.lang`을 갱신하는 effect 추가**

`display-settings-applier.tsx`의 기존 두 effect 다음에 세 번째 effect 추가:

```typescript
// Sync html lang attribute
useEffect(() => {
  document.documentElement.lang = display.locale;
}, [display.locale]);
```

(파일 최종 모습 예시 — `DisplaySettingsApplier` 함수 전체):

```typescript
'use client';

import { useEffect } from 'react';
import { useTheme } from 'next-themes';
import { useSettingsStore } from '@/lib/stores/settings-store';

const FONT_SIZE_CLASS: Record<string, string> = {
  small: 'font-sm',
  medium: 'font-md',
  large: 'font-lg',
};

const DENSITY_CLASS: Record<string, string> = {
  compact: 'density-compact',
  comfortable: 'density-comfortable',
  spacious: 'density-spacious',
};

export function DisplaySettingsApplier() {
  const display = useSettingsStore((s) => s.display);
  const { setTheme } = useTheme();

  useEffect(() => {
    setTheme(display.theme);
  }, [display.theme, setTheme]);

  useEffect(() => {
    const html = document.documentElement;
    Object.values(FONT_SIZE_CLASS).forEach((cls) => html.classList.remove(cls));
    html.classList.add(FONT_SIZE_CLASS[display.font_size] ?? 'font-md');

    Object.values(DENSITY_CLASS).forEach((cls) => html.classList.remove(cls));
    html.classList.add(DENSITY_CLASS[display.content_density] ?? 'density-comfortable');
  }, [display.font_size, display.content_density]);

  useEffect(() => {
    document.documentElement.lang = display.locale;
  }, [display.locale]);

  return null;
}
```

- [ ] **Step 2: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/display-settings-applier.tsx
```
Expected: 오류 없음.

- [ ] **Step 3: 브라우저 수동 검증**

```bash
npm run dev
```
- 초기 진입: DevTools Elements 탭에서 `<html lang="ko">` 확인
- 헤더에서 English로 토글 → `<html lang="en">` 으로 변경됨 확인
- 새로고침 → 마운트 직후 `<html lang="en">` 으로 동기화 (SSR `ko` → 클라이언트 갱신; suppressHydrationWarning이 이미 켜져 있어 경고 없음)

- [ ] **Step 4: 커밋**

```bash
git add src/components/layout/display-settings-applier.tsx
git commit -m "feat(i18n): sync html lang attribute with locale"
```

---

### Task 12: NotificationBell 칭크롬 번역

**Files:**
- Modify: `src/components/layout/notification-bell.tsx`

- [ ] **Step 1: import 추가**

```typescript
import { useT } from '@/lib/i18n/use-translation';
```

- [ ] **Step 2: `timeAgo` 함수를 hook 내부로 옮기거나 locale-aware로 변경**

기존 `timeAgo`는 외부 함수이므로 hook을 쓸 수 없다. `NotificationBell` 컴포넌트 안에서 사용되도록 클로저로 만든다. 파일 상단의 `function timeAgo(iso: string): string` 정의를 **삭제**하고, `NotificationBell` 컴포넌트 본문 내부에 `const t`를 받은 뒤 다음 함수를 정의:

```typescript
const { t, locale } = useT();

const timeAgo = (iso: string): string => {
  const now = new Date();
  const then = new Date(iso);
  const diffMs = now.getTime() - then.getTime();
  const diffMin = Math.floor(diffMs / (1000 * 60));
  if (diffMin < 1) return t('notification.timeAgo.justNow');
  if (diffMin < 60) return t('notification.timeAgo.minutes', { count: diffMin });
  const diffHour = Math.floor(diffMin / 60);
  if (diffHour < 24) return t('notification.timeAgo.hours', { count: diffHour });
  const diffDay = Math.floor(diffHour / 24);
  if (diffDay < 7) return t('notification.timeAgo.days', { count: diffDay });
  return then.toLocaleDateString(locale === 'ko' ? 'ko-KR' : 'en-US');
};
```

- [ ] **Step 3: 알림 칭크롬 라벨 모두 `t()`로 교체**

다음 위치를 차례로 변경:

1. `<span className="font-semibold text-sm">알림</span>` → `<span className="font-semibold text-sm">{t('notification.title')}</span>`

2. `{unreadCount}개 미확인` (Badge 내부) → `{t('notification.unreadBadge', { count: unreadCount })}`

3. `모두 읽음` 버튼 텍스트 → `{t('notification.markAllRead')}`

4. `window.confirm('모든 알림을 삭제하시겠습니까?')` → `window.confirm(t('notification.clearAllConfirm'))`

5. `알림이 없습니다.` → `{t('notification.empty')}`

- [ ] **Step 4: 타입 체크 및 lint 통과**

```bash
npx tsc --noEmit && npx eslint src/components/layout/notification-bell.tsx
```
Expected: 오류 없음.

- [ ] **Step 5: 브라우저 수동 검증**

```bash
npm run dev
```
- 헤더의 종(Bell) 아이콘 클릭 → 드롭다운 헤더 `알림` 표시. 영문 토글 시 `Notifications`로.
- 미확인 알림이 있으면 `N개 미확인` ↔ `N unread` 변경
- `모두 읽음` ↔ `Mark all read`
- 알림이 없을 때 `알림이 없습니다.` ↔ `No notifications.`
- 알림 항목의 `방금/X분 전/X시간 전/X일 전` ↔ `just now/Xm ago/Xh ago/Xd ago`

> 알림이 없는 상태로 검증이 어렵다면, `useNotificationStore`에 임시 알림을 추가하거나 localStorage `hrms-notifications`를 수동 편집해 확인.

- [ ] **Step 6: 커밋**

```bash
git add src/components/layout/notification-bell.tsx
git commit -m "feat(i18n): translate notification bell chrome and timeAgo"
```

---

### Task 13: 최종 검증

**Files:** (변경 없음 — 검증만)

- [ ] **Step 1: 전체 빌드 통과 확인**

```bash
npm run lint
```
Expected: 오류 없음.

```bash
npm run build
```
Expected: build success.

- [ ] **Step 2: 영문 사전 누락 폴백 동작 수동 테스트**

`src/lib/i18n/dictionaries/en.ts`에서 임시로 키 하나 삭제 (예: `'menu.dashboard': 'Dashboard',` 줄을 주석 처리).

```bash
npx tsc --noEmit
```
Expected: `Type '...' is missing the following properties from type 'Record<...>': "menu.dashboard"` 컴파일 에러.

→ 타입 시스템이 누락을 막아준다는 것 확인.

복원하고 다시 컴파일 OK.

- [ ] **Step 3: 런타임 폴백 확인 (선택)**

영문 사전에서 한 키의 값을 빈 문자열이 아니라 `undefined`로 만들 수는 없으나(타입 강제), 런타임 폴백 로직을 보고 싶다면 `use-translation.ts`에서 `DICTIONARIES[locale][key]`를 임시로 `undefined`로 만들어 `console.warn`이 뜨는지 확인 후 복원.

- [ ] **Step 4: persist round-trip 확인**

브라우저에서:
1. EN으로 토글
2. 페이지 새로고침 → 여전히 영문
3. DevTools Application → Local Storage → `hrms-settings` → `state.display.locale === 'en'` 확인
4. 한국어로 토글 → `state.display.locale === 'ko'`

- [ ] **Step 5: 시나리오 시연 — 전 영역 토글 확인**

`/`, `/employees`, `/leave`, `/settings` 페이지를 차례로 방문하며 KO↔EN 토글을 반복:
- 사이드바: 메뉴 그룹/항목 모두 정상 전환
- 헤더: 검색·내정보·로그아웃·역할·언어 토글 자체 라벨 정상 전환
- NotificationBell: 알림 헤더/배지/시간 정상 전환
- `<html lang>` 속성도 일치
- 페이지 본문은 이번 작업의 비범위이므로 한글 그대로 — **이는 정상**

- [ ] **Step 6: 최종 커밋 (필요한 경우)**

이번 Task에서 코드 변경이 없다면 커밋 없음. 검증만으로 끝.

---

## Done Criteria

- [ ] 모든 Task의 step이 체크됨
- [ ] `npm run lint` 통과
- [ ] `npm run build` 통과
- [ ] 헤더의 LanguageToggle로 KO ↔ EN 즉시 전환됨
- [ ] 사이드바·헤더·알림벨 칭크롬이 모두 번역됨
- [ ] 새로고침 후 선택한 언어 유지 (persist)
- [ ] `<html lang>` 속성이 locale에 맞게 갱신됨
- [ ] DevTools Console에 `[i18n] missing` 경고 없음
